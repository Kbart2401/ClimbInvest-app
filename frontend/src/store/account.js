import { fetch } from './csrf';

//action types
const SET_ACCOUNT = 'SET_ACCOUNT'

//action creators



/**********Account Reducer**********/
export const accountUpdateReducer = (state={}, action) => {
  switch(action.type) {
    default:
      return state
  }
}